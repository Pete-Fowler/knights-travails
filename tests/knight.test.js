import Knight from "../tests/knight.test.js";

let k = new Knight();

describe("Setup/Helpers", () => {
  describe("possibleMoves", () => {
    it("Returns valid moves only", () => {
      expect(k.)
    });
  });
});
